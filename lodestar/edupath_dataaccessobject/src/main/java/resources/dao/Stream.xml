<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Stream">

	 <resultMap type="com.lodestar.edupath.datatransferobject.dto.elective.StreamsDTO" id="SelectResult"/>
	 <parameterMap type="com.lodestar.edupath.datatransferobject.dto.elective.StreamsDTO" id="inputParam"/>
	 
	<select id="getAllStream" resultMap="SelectResult">
		SELECT DISTINCT * FROM Streams ORDER BY name
	</select>
	
</mapper>